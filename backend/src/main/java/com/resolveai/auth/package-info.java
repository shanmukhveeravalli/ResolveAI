/**
 * Authentication Module (Bounded Context).
 * 
 * Scope: User registration, login, stateless JWT generation and validation,
 * refresh token rotation, and security principal extraction.
 * Implementation Milestone: Phase 3.
 */
package com.resolveai.auth;
